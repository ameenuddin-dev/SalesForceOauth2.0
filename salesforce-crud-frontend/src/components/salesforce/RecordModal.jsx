import {useEffect,useState} from 'react';
export default function RecordModal({open,mode,objectConfig,record,onClose,onSubmit,submitting}){const [form,setForm]=useState({});useEffect(()=>{const x={};objectConfig.fields.forEach(f=>x[f.name]=record?.[f.name]??'');setForm(x)},[objectConfig,record,open]);if(!open)return null;const submit=async e=>{e.preventDefault();const data={};objectConfig.fields.forEach(f=>{if(form[f.name]!==''&&form[f.name]!=null)data[f.name]=f.type==='number'?Number(form[f.name]):form[f.name]});await onSubmit(data)};return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><form onSubmit={submit} className="w-full max-w-2xl rounded-2xl bg-white p-6"><div className="mb-5 flex justify-between"><h2 className="text-xl font-bold">{mode==='edit'?'Edit':'Create'} {objectConfig.singular}</h2><button type="button" onClick={onClose}>✕</button></div><div className="grid gap-4 sm:grid-cols-2">{objectConfig.fields.map(f=><label key={f.name} className={f.type==='textarea'?'sm:col-span-2':''}><span className="mb-1 block text-sm font-medium">{f.label}{f.required&&' *'}</span>{f.type==='textarea'?<textarea name={f.name} rows="4" required={f.required} value={form[f.name]??''} onChange={e=>setForm({...form,[f.name]:e.target.value})} className="w-full rounded-lg border p-2"/>:<input name={f.name} type={f.type} required={f.required} value={form[f.name]??''} onChange={e=>setForm({...form,[f.name]:e.target.value})} className="w-full rounded-lg border p-2"/>}</label>)}</div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onClose} className="rounded-lg border px-4 py-2">Cancel</button><button disabled={submitting} className="rounded-lg bg-blue-600 px-5 py-2 text-white">{submitting?'Saving...':mode==='edit'?'Update':'Create'}</button></div></form></div>}
